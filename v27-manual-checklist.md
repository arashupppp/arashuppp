# چک‌لیست تست دستی HOVI — v27 (فاز ۱)

راهنما: به ترتیب برو، هر مرحله اول **کن** بعد **بیاید**. اگر گزینه‌ای سبز نشد، همان شماره را به من بگو.
فایل: `hovi_editor-v27.html` — همه تست‌ها روی **یک پروژهٔ جدید** انجام می‌شود و آخرش ذخیره می‌کنی.

## ۰) آماده‌سازی (۱ دقیقه)
- [ ] فایل را باز کن — بج بالا-چپ باید بنویسد `editor-v27`
- [ ] روی بوم یک کلیک وسط بگیر — هیچ خطایی نباید بیاید
- [ ] عکس پلان (کالیبره‌شده) اگر داری بارگذاری کن: دکمهٔ 🖼 عکس پلان

## ۱) دیوار و کالیبراسیون
- [ ] کلید `2` یا دکمهٔ 📐 دیوار → ۴ کلیک دور یک اتاق (مثلاً ۹×۷) — کلیک‌ها زنجیره‌ای وصل شوند
- [ ] Shift موقع کشیدن = گونیا (خط افقی/عمودی قفل شود)
- [ ] Esc یا راست‌کلیک = پایان زنجیره
- [ ] 🎯 کالیبراسیون: دو سر یک فاصلهٔ معلوم کلیک، عدد واقعی را بده — بعد دیوارهای جدید هم‌مقیاس شوند

## ۲) بازشوها (۴ نوع)
- [ ] دکمهٔ 🚪 درب ▾ → تک‌لنگه: کلیک روی یک دیوار → قوس درب رسم شود
- [ ] دولنگه، کشویی (دو خط موازی)، بازشوی بدون درب — هر کدام یکی نصب کن
- [ ] دبل‌کلیک روی درب → پنل ویرایش باز شود (بدون پاپ‌آپ خام!) — عرض/جای‌گاه/لولا/قوس را عوض کن → تأیید → تغییر اعمال شود
- [ ] کلید `F` روی درب انتخابی = آینه‌کردن لولا
- [ ] 🪟 پنجره (`5`): نصب روی دیوار + دبل‌کلیک = پنل

## ۳) ستون و آسانسور
- [ ] ستون (`6`): Alt = مگنت به سر دیوار، بدون Alt = جای آزاد — نصب شود
- [ ] Space موقع درگ ستون = چرخش
- [ ] درگ = جابجایی، `Del` = حذف، `Ctrl+Z` = برگشت
- [ ] آسانسور (دکمهٔ ⬛): کلیک = شفت؛ `R` = چرخش ۹۰°

## ۴) پله (v23 + پنل v25)
- [ ] 🪜 پله ▾ → مستقیم: ۲ کلیک — فلش UP و خطوط پله
- [ ] دربست L و U و J: ۳ کلیک — شکل درست
- [ ] مارپیچ: ۱ کلیک — دایره + شعاع
- [ ] **دبل‌کلیک روی پله → پنل ویرایش پله باز شود** (کارت فیروزه‌ای وسط)
  - [ ] تغییر نوع (مستقیم → U و...) → شکل عوض شود
  - [ ] عرض/ارتفاع/جهت صعود → تأیید → اعمال
  - [ ] Esc پنل را ببندد
- [ ] درگ پلهٔ مارپیچ → جابجا شود بدون خطا (قبلاً کرش داشت)

## ۵) نورباز (v24)
- [ ] ☀️ نورباز: ۲ کلیک (گوشه اول/مقابل) → مستطیل خط‌چین + ☀
- [ ] درگ جابجایی، Del، Ctrl+Z

## ۶) بالکن و نرده (v26)
- [ ] ⬒ بالکن (`7`): ۳-۴ کلیک بیرونِ دیوار → Enter → هاشور کاشی + خط نرده دولا + برچسب «بالکن»
- [ ] ⌒ نرده (`9`): ۲-۳ کلیک (مثلاً لبهٔ پله) → راست‌کلیک = پایان → نماد دولای
- [ ] Esc وسط نصب = لغو کامل (چیزی ثبت نشود)
- [ ] درگ کل بالکن/نرده = جابجایی

## ۷) ناحیه/Zone (v27)
- [ ] ⬚ ناحیه (`8`): ۴ کلیک داخل اتاق → Enter → رنگ نیمه‌شفاف + **نام + مساحت m²** در مرکز
- [ ] دبل‌کلیک روی ناحیه → پنل «ویرایش ناحیه» → نام «پذیرایی» + رنگ عوض کن → تأیید → برچسب عوض شود
- [ ] حذف از پنل → Ctrl+Z برگرداند
- [ ] مساحت را با حساب سرانگشتی چک کن (مثلاً ۳×۴ = ۱۲ m²)

## ۸) انتخاب، قفل، حذف
- [ ] `~` روی شیء انتخابی = قفل (سعی در درگ = پیام «قفل است») — دوباره `~` = باز
- [ ] `Del` روی هر نوع شیء = حذف
- [ ] چند بار `Ctrl+Z` پشت‌سرهم = همهٔ مراحل برگردد (دیوار، درب، پله، ناحیه...)
- [ ] درگ بوم با موس وسط = pan؛ اسکرول = زوم

## ۹) ذخیره/بارگذاری
- [ ] 💾 ذخیره → فایل `.hovi.json` دانلود شود
- [ ] 📂 بازکردن همان فایل → همه‌چیز سرجایش باشد (تعداد: دیوارها، درب‌ها، پله‌ها، بالکن، نرده، ناحیه با نام/رنگ)

## ۱۰) خروجی‌ها
- [ ] ⬇ GLTF → فایل GLB دانلود شود؛ در https://gltf-viewer.donmccurdy.com باز کن — باید دیوار/پله/نردهٔ میله‌ای/کف بالکن/کف رنگی ناحیه دیده شود
- [ ] 🏗 ساخت تور → فایل HTML تور باز کن → داخل آن راه برو (کلیک) — از روی پلهٔ مستقیم بالا برو — بالکن قابل راه‌رفتن

## ⚠️ اگر خطا دیدی
متن خطا یا اسکرین‌شات + شمارهٔ مرحله را بفرست. تست‌های خودکار من همهٔ این‌ها را پوشش می‌دهند و سبزند؛ هدف این چک‌لیست **حس کاربر واقعی** است.
