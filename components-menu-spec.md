# HOVI — مشخصات زیرمنو و پارامترهای اجزای ساختمانی (پلان ۲بعدی)

قاعده کلی: هر پارامتری که در خروجی سه‌بعدی/هندسه واقعی اثر دارد → **قابل ویرایش عددی**؛ هر چیزی که فقط نحوهٔ رسم سمبل در پلان است → **فقط بصری**.
الگوی تعامل هماهنگ با وضع موجود: درب/پنجره = کلیک روی دیوار؛ ستون = ۴کلیک + چرخش R (۱۵°)؛ همهٔ اجزای آزاد = نصب ۱کلیکی با آهنربای دیوار + `R` چرخش + `F` آینه + دابل‌کلیک = پنجرهٔ ابعاد عددی + درگ جابه‌جایی. همهٔ ابعاد clamp دارند (مثل بازشوها).

---

## ۱) درب
| زیرمنو/پارامتر | قابل ویرایش | فقط بصری |
|---|---|---|
| نوع: تک‌لنگه / دوتلنگه / کشویی (یک‌ریل یا دورِریل) | ✔ از منوی انتخاب (تعویض نوع، سمبل بازترسیم شود) | — |
| عرض | ✔ عددی (۶۰–۱۲۰cm؛ دوتلنگه تا ۱۸۰) + درگ دو سر بازشو | — |
| جهت بازشو (داخل/خارج) | ✔ فلپ ۱۸۰° حول دیوار (دکمه یا کلید) | — |
| سمت لولا (راست/چپ) | ✔ آینه `F` | — |
| موقعیت روی دیوار (t) | ✔ درگ در طول دیوار (کلمپ موجود) | — |
| قوس بازشدگی ۹۰° | — | ✔ خودکار از نوع/لولا/جهت رسم می‌شود |
| جنس، رنگ، دستگیره، ضخامت برگه | — | ✔ ثابت در سمبل (فعلاً پارامتر نیست) |

نصب: همان «کلیک روی دیوار». پیش‌فرض عرض ۹۰، داخل‌رو، لولا چپ.

## ۲) پنجره
| پارامتر | قابل ویرایش | فقط بصری |
|---|---|---|
| نوع: کشویی / فولادی (دوتیکه) — بعداً UPVC/کاذقوس | ✔ از منو (تعداد میله‌ها از نوع مشتق می‌شود) | خطوط میلهٔ وسط (mullion) خودکار |
| عرض | ✔ موجود: ۲کلیکی + دابل‌کلیک عددی + درگ دو سر | — |
| ارتفاع | ✔ فقط عددی (در پلان دیده نمی‌شود؛ در ۳D اثر دارد) | — |
| ارتفاع از کف (سیل/دست‌انداز) | ✔ فقط عددی | — |
| ضخامت فریم | ✔ عددی — اثر واقعی فقط در ۳D؛ در پلان فقط ضخامت خط نمایشی | — |

پیشنهاد: دابل‌کلیک = پنجرهٔ ۳فیلدی (عرض/ارتفاع/سیل). پیش‌فرض: ۱۲۰×۱۲۰، سیل ۹۰cm.

## ۳) پله
| پارامتر | قابل ویرایش | فقط بصری |
|---|---|---|
| نوع: مستقیم / دربست L / دربست U / مارپیچ | ✔ از منو | — |
| ابعاد: مستقیم = طول×عرض؛ L/U = دو بازو×عرض کف | ✔ عددی + درگ سرِ بازوها | — |
| قطر مارپیچ | ✔ عددی (۱۲۰–۲۰۰cm) | — |
| جهت چرخش مارپیچ | ✔ راست‌گرد/چپ‌گرد | — |
| جهت ورود/چرخش کل | ✔ `R` + `F` | — |
| تعداد/ارتفاع پله‌ها | ✔ عددی (برای ۳D و لیبل) | — |
| خطوط پله‌ها (گوه) و فلش مسیر صعود | — | ✔ خودکار از هندسه و جهت ورود |

نصب پیشنهادی: مستقیم = ۲کلیک (شروع + جهت/طول)؛ مارپیچ = ۱کلیک + قطر عددی. مسیر در پلان باید داخل تیغه‌های مسکونی بماند (کلمپ حداقل عرض ۸۰cm).

## ۴) آسانسور
| پارامتر | قابل ویرایش | فقط بصری |
|---|---|---|
| ابعاد شفت عرض×عمق | ✔ عددی (پیش‌فرض ۱۶۰×۱۷۰؛ کابین ۱۰۰×۱۲۰) | — |
| جهت درب (N/S/E/W) | ✔ چرخش `R` (۹۰° پله‌ای اینجا بهتر از ۱۵°) | — |
| تعداد توقف | ✔ عددی (فقط لیبل/۳D) | — |
| موقعیت | ✔ درگ (چسب به دیوار) | — |
| X قطری داخل شفت + خط کابین | — | ✔ سمبل استاندارد، خودکار |

نصب: درگ مستطیل یا ۲کلیک (دو گوشهٔ شفت).

## ۵) شومینه
| پارامتر | قابل ویرایش | فقط بصری |
|---|---|---|
| عرض×عمق | ✔ عددی + درگ | — |
| جهت رو به فضا | ✔ `R` | — |
| نوع: دیواری / گوشه / جزیره | ✔ (اختیاری، فاز ۲) | — |
| قفسه، دودکش، شعله | — | ✔ فقط سمبل/۳D |

نصب: ۱کلیک + آهنربای دیوار. پیش‌فرض ۱۲۰×۴۰.

## ۶) کابینت
| پارامتر | قابل ویرایش | فقط بصری |
|---|---|---|
| نوع/شکل: خطی / L / U / جزیره | ✔ از منو (مسیر تغییر کند، خط سیر بازترسیم شود) | — |
| طول | ✔ عددی + درگ انتهای کانتر در راستای دیوار (الگوی ۲کلیکی پنجره) | — |
| عمق | ✔ عددی (کانتر ۶۰، آویز ۳۵) | — |
| سمت (کدام سمت دیوار) | ✔ آینه `F` | — |
| درز درب‌ها، سینک/گاز/یخچال‌دان | — | ✔ در پلان سمبل ثابت؛ سینک و اجاق اگر خواست بعداً آبجکت جدا می‌شوند |

نصب: ۲کلیک روی مسیر دیوار (شروع و پایان) + درگ انتها.

## ۷) سرویس بهداشتی (مشترک: نصب ۱کلیک + `R` + `F` + درگ + دابل‌کلیک ابعاد عددی)
| جزء | قابل ویرایش | فقط بصری |
|---|---|---|
| روشویی | عرض ۵۰–۷۰، عمق، نوع (روپایه/کابینی/گوشه)، جهت | شکل کاسه، شیر |
| توالت فرنگی | نوع (زمینی/آویز)، عرض ~۳۸، جهت؛ فاصله از دیوار پشت خودکار | شکل مخزن و درِ فلاش‌تانک |
| توالت ایرانی | ابعاد صفحه ~۴۰×۶۰، جهت | شیب/خروجی آب |
| دوش (زیردوشی/کابین) | ضلع ۶۰–۱۰۰ یا قطر (گرد)، شکل مربع/گرد، جهت | شیشه و نازل |
| وان | طول×عرض ۱۷۰×۷۰، جهت، نوع (دوش‌دار) | شیرها و شیب داخلی |
| جکوزی | ابعاد یا قطر، شکل (مربع/گرد/بیضی)، جهت | جت‌ها، پلهٔ داخلی، خطوط لوله |

سمبل‌ها از یک کتابخانهٔ سمبل‌نقشه‌کشی می‌آیند؛ شکل سمبل پارامتر نیست.

## ۸) مبلمان ثابت (کمد دیواری، جاکفشی، ست تلویزیون، میز ثابت، کتابخانه)
| پارامتر | قابل ویرایش | فقط بصری |
|---|---|---|
| ابعاد عرض×عمق | ✔ عددی + درگ دو سر | — |
| چرخش/آینه | ✔ `R` ۱۵° + `F` | — |
| نوع | ✔ از گالری انتخاب (تعویض آبجکت) | — |
| آهنربای دیوار (کمد/جاکفشی) | ✔ on/off با درگ | — |
| خطوط داخلی (طبقات، لباس، لولا) | — | ✔ فقط سمبل کتابخانه |

نصب: درگ‌اندروپ از گالری (۱کلیک).

---

## جمع‌بندی کلی
- **قابل ویرایش عددی**: همهٔ ابعاد (عرض/ارتفاع/عمق/قطر/طول)، موقعیت (درگ روی دیوار یا در فضا)، جهت (چرخش/آینه/فلپ)، نوع (منوی تعویض)، و در پله/آسانسور: ارتفاع و تعداد توقف.
- **فقط بصری**: سمبل‌نگاری پلان (قوس درب، فلش پله، X آسانسور، خطوط پله)، جزئیات داخلی سمبل‌ها (کاسه، شیر، جت، طبقات)، و پارامترهای صرفاً سه‌بعدی (ضخامت فریم پنجره، دودکش شومینه) که عددی ذخیره می‌شوند ولی در پلان فقط خط نمایشی‌اند.
- الگوی یکسان هر آبجکت: **نصب (۱ یا ۲کلیک) ← انتخاب ← تولبار شناور: [نوع] [ابعاد] [چرخش R] [آینه F] [قفل] [حذف]** — همین ساختار برای درب/پنجره/ستون هم تعمیم می‌یابد تا UI ناهمگون نشود.
