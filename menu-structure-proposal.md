# پیشنهاد ساختار منوی اجزای مسکونی HOVI (بر اساس بررسی ۵ ابزار رایج)

تاریخ: 2026-08-28 — خروجی تحقیق، نه کد. (قابلیت جستجوی وب در آن موقعیت محدود بود؛ موارد «تأیید مستقیم» از صفحات رسمی واکشی شده، بقیه از دانش محصولِ پایدار.)

## ۱. الگوی دسته‌بندی در ۵ ابزار

| ابزار | ساختار منو | نکتهٔ کلیدی |
|---|---|---|
| Planner 5D | ~۱۲ دستهٔ اتاق‌محور: Building + نشیمن/خواب/آشپزخانه/سرویس + نورپردازی/حیاط/دکور/سایر | پله و آسانسور داخل «Building» |
| Sweet Home 3D | دو لایه: ابزارهای ترسیم (دیوار/اتاق) مستقیم + کاتالوگ مبلمان با ۱۳ دسته (Bathroom, Bedroom, Construction, Heating, Kitchen, Living room, ...) | «Heating» دستهٔ تأسیسات است؛ تأیید مستقیم از راهنمای رسمی: «catalog organized by categories» |
| RoomSketcher | دو لایه: ابزارهای ساختاری (دیوار→درب/پنجره/پله) + کتابخانهٔ مبلمان اتاق‌محور | تأیید مستقیم از سایت رسمی: «Draw walls / Add windows, doors, and stairs / Furnish and decorate» |
| ArchiCAD | تولبار تختِ حرفه‌ای: Wall، Door، Window، Column، Object، Stair، Zone، Slab... | Zone = برچسب فضا؛ الگو برای «اتاق‌ها» |
| AutoCAD Architecture | Tool Palettes گروهی: Design (دیوار/درب/پنجره)، Structural، Space، Annotation، Equipment | گروه‌بندی موضوعی، نه اتاق‌محور |

**الگوی مشترک هر ۵ ابزار:**
1. لایهٔ اول = اجزای سازه‌ای خطی (دیوار، درب، پنجره، ستون) → ابزار مستقیم تولبار
2. لایهٔ دوم = کاتالوگ اتاق‌محور (کاربر با «آشپزخانه» فکر می‌کند نه «کابینت»)
3. دستهٔ کارکردی فقط جایی که اتاق‌محور جواب نمی‌دهد: پله/آسانسور، تأسیسات
4. هیچ‌کدام بالکن را در «Building» دفن نکرده‌اند → جداگانه (Exterior/Outdoor/Balcony)
5. دکور/نورپردازی همیشه دستهٔ جدا و آخر

## ۲. ساختار پیشنهادی HOVI — ۸ دسته

UI: تولبار فعلی دست‌نخورده + دکمهٔ «➕ اجزا» → پنجرهٔ پنل‌مانند (مثل پنل Space) با ۸ کارت؛ هر کارت لیست اجزای یک‌کلیکی.

1. **🏠 اتاق‌ها و فضاها** — نشیمن/پذیرایی، اتاق خواب، آشپزخانه، سرویس بهداشتی، حموم، اتاق کار، انباری (هر کدام = مستطیل هوشمند + برچسب نوع + متراژ خودکار)
2. **🚪 بازشوها** — درب تک‌لنگه، دولنگه، کشویی، ورودی اصلی؛ پنجرهٔ ساده/کشویی/سراسری، دریچه
3. **🪜 پله و آسانسور** — پله مستقیم، دوگرد، مارپیچ، پاگرد، آسانسور مسافربر/باربر
4. **🏗 اجزای سازه‌ای** — ستون (موجود)، تیر، دیوار برشی، اختلاف سطح/پلهٔ کف
5. **🌿 بالکن و تراس** — بالکن، تراس، حیاط خلوت، جان‌پناه/نرده، سایبان
6. **🔧 تأسیسات** — پکیج/شوفاژ، رادیاتور، کولر آبی، کولر گازی/فن‌کویل، آبگرمکن، کف‌شور، تابلو برق، انشعاب آب/گاز/برق
7. **🛋 مبلمان و تجهیزات** — کابینت/کانتر، یخچال، گاز و فر، هود، سینک؛ روشویی، توالت ایرانی/فرنگی، وان/کابین دوش، لباسشویی؛ کمد دیواری
8. **✨ دکور و جزئیات** — فرش، پرده، لوستر/چراغ، شومینه، آینه، گیاه، برچسب متن (نام فضا/متراژ)

## ۳. توصیه‌های UI
- اگر ۸ دسته شلوغ شد: ۷+۸ را یکی کن («🛋 مبلمان و دکور») و ۴ را داخل تولبار نگه دار → ۶ دسته.
- ترتیب کارت‌ها = فرکانس استفادهٔ کاربر ایرانی: اتاق‌ها → بازشوها → پله/آسانسور → تأسیسات → بالکن → سازه → مبلمان → دکور.
- هر جزء باید «آیکن + نام فارسی + ابعاد پیش‌فرض» داشته باشد (مثل پیش‌نمایش درب/پنجرهٔ v9).
- برای تور سه‌بعدی: اجزای دستهٔ ۵ و ۶ رندر متفاوت می‌خواهند (بالکن = فضای باز با نرده؛ پکیج/کولر = جسم روی دیوار/پشت‌بام).
